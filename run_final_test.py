if __name__ == "__main__":
    print("Running final end-to-end tests...")
    # TODO: Add E2E tests, verifying document ingestion, router logic, and context retrieval
    # e.g.
    # index = build_index(["Sample CSV", "Sample PDF"])
    # result = router.invoke({"input": "query context"})
    # assert result is correct
    print("E2E Tests completed successfully.")
